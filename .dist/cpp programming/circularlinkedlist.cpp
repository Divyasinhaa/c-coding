#include <iostream>
using namespace std;
class node{
    public:
    int data;
    node *prev;
    node *next;

    //constructor
    node(int d){
        this->data=d;
        this->prev=NULL;
        this->next=NULL;
    }

};
void insertion(node *&head,int d,){
    node *new node=new data(d);
    //empty list
    if(head==NULL)
    while(data>1){
        temp->next=head;
        head->prev=head;

    }
}